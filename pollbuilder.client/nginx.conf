server {
    listen 80;
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 🟢 THIS TELLS NGINX: If you can't find the file, load index.html!
        try_files $uri $uri/ /index.html; 
    }
}